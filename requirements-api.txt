# Serving-only dependencies for the HTTP API / container image.
# Intentionally excludes pandas (used only by the eval harness, not at serve
# time) so the deployed image stays lean.
anthropic
fastapi
pydantic
uvicorn[standard]
# classify.py imports telemetry.py unconditionally, so the OTel API must be
# present for the app to import at all. The SDK is only imported inside
# setup_tracing(), but pyproject.toml treats both as core so the
# no-op-when-disabled path works out of the box; keep the image consistent with
# it. The OTLP wire exporter stays an optional extra and is not installed here.
opentelemetry-api
opentelemetry-sdk
